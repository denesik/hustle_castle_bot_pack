{	
	"debug_log": 0, 
	
	// Image capture mode from the emulator.
	// 0 - should work on all computers. It is necessary that the emulator window is on top of all windows.
	// 1 - works on some computers. The emulator window may be blocked by other windows, but should not be minimized.
	// Режим захвата картинки с эмулятора.
	// 0 - должен работать на всех компьютерах. Нужно что-бы окно эмулятора было поверх всех окон.
	// 1 - работает на некоторых компьютерах. Окно эмулятора может быть перекрыто другими окнами, но не должно быть свернуто.
	"capute_mode": 1, 			
	
	// Сколько часов хранится запись в blacklist
	"blacklist_hours": 72, 
	
	// If the emulator does not respond to the esc signal that the bot sends, try setting the value 1 to this field.
	// Если эмулятор не реагирует на сигнал esc, который посылает бот, попробуйте поставить значение 1 в данном поле.
	"esc_additional_mode": 0,
	
	
	// The interval through which the screen is scanned in ms
	// Интервал, через который сканируется экран в мс
	"pause": 500, 						
	
	// How much time is available for one state in ms.
	// Сколько времени доступно на одно состо¤ние в мс.
	"short_state_timeout": 10000, 	
	"long_state_timeout": 200000,		
	"arena_find_timeout": 600000,		
	"recovery_state_timeout": 300000,	// 5 минут, потом уходим в ребут.	// 5 minutes, then go into reboot.
	
	
	// Time to complete and start the emulator in ms.
	// Время на завершение и запуск эмулятора в мс. 
	"emulator_stop_time": 60000, 
	"emulator_start_time": 120000, 		
	
	// Path to the Nox emulator. Slashes should be double !!! (\\) - 2 slashes and not 1.
	// Путь к эмулятору Nox. Слеши должны быть двойные!!! (\\) - 2 слеша а не 1.
	"nox_path": "D:\\Program Files\\Nox\\bin\\Nox.exe",
}
